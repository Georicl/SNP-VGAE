[project]
name = "gat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.0",
    "torch>=2.12.1",
    "pytest>=8.0.0",
    "tqdm>=4.66.0",
]

[tool.setuptools.packages.find]
where = ["pytorch/src"]
